//this is also the implementation of the q3) where we 
//have create a class inside a package while importing another package

package calculator.logarithmic;
import java.lang.Math;
public class sine
{
    public double sineVal(double x)
    {
        return Math.sin(Math.toRadians(x));
    }
}